// Kept in its own lazy bundle so visitors who never export a ghostwriter
// thought do not pay for the encoder during the cinematic intro.
export { GIFEncoder, quantize, applyPalette } from 'gifenc';
